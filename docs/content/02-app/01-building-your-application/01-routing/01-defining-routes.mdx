---
title: 自定义路由
description: 了解如何在Next.js中创建您的第一个路由。
related:
  description: 了解更多关于创建页面和布局
  links:
    - app/building-your-application/routing/pages
---

> 在开始前我们建议您先阅读[路由基本概念](/docs/content/app/building-your-application/routing)页面。

本页面将会指导您如何定义和组织 Next.js 应用程序中的路由。

## 创建路由

Next.js 使用基于文件系统的路由器，其中**文件夹**用于定义路由。

每个文件夹代表一个映射到**URL**的 [**路由** 段](/docs/content/app/building-your-application/routing#route-segments)。 要创建[嵌套路由](/docs/content/app/building-your-application/routing#nested-routes), 您可以将文件夹嵌套在彼此内部。

<img
  alt="Route segments to path segments"
  src="/route-segments-to-path-segments.avif"
  width="100%"
  height="auto"
/>

一个专门的[`page.js`文件](/docs/content/app/building-your-application/routing/pages)用于使路由段可公开访问。

<img
  alt="Defining Routes"
  src="/defining-routes.avif"
  width="100%"
  height="auto"
/>

在这个图示中, 不能够公开访问`/dashboard/analytics` URL 路径，因为它没有对应的`page.js`文件。可以使用此文件夹来存储组件、样式、图片或其它共享文件。

> **您需要知道**: 特殊文件可以使用`.js`, `.jsx`, or `.tsx`文件扩展名。

## 创建 UI

[特殊文件约定](/docs/content/app/building-your-application/routing#file-conventions) 用于为每个路由段创建 UI。最常见的是显示唯一路由的 UI[页面](/docs/content/app/building-your-application/routing/pages), 及显示多个路由共享的 UI [布局](/docs/content/app/building-your-application/routing/layouts-and-templates#layouts)。

例如, 要创建您的第一个页面，您需要在`app`目录下添加一个`page.js`文件并且在此文件中 export 一个 React 组件:

```tsx filename="app/page.tsx" switcher
export default function Page() {
  return <h1>Hello, Next.js!</h1>;
}
```
